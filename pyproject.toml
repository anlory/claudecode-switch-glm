[project]
name = "cc_glm"
version = "0.1.1"
description = "管理 Claude Code 的 GLM 环境变量配置，一键切换智谱 GLM 模型"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
cc-glm = "main:main"
