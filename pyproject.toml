[project]
name = "cc_switch"
version = "0.1.1"
description = "管理 Claude Code 模型配置，支持多模型切换"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
cc-switch = "main:main"
